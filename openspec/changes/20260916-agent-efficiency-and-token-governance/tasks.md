# Tareas — 20260916-agent-efficiency-and-token-governance

estado: en_progreso

## Invocaciones planificadas
- `python-data-engineer` (1 sesión): `efficiency.py` + CLI + manifest + methodology.md + tests.
- `data-science-reviewer` (1 sesión, solo lectura).
- `python-data-engineer` (fixes, hasta 2 ciclos).
- Lead: tests reales, manifest parity, doctor, commit local.

## Tareas
- [ ] `tools/dsguard/efficiency.py`.
- [ ] `tools/ds_guard.py`: subcomando `efficiency report`.
- [ ] `tools/ds_init/manifest.py`: entrada VERBATIM (discovery, sin stage_minimo — mismo criterio
      que `sdd.py`, es parte del núcleo SDD).
- [ ] `.claude/skills/lead-data-scientist/methodology.md` + `.tmpl`: sección nueva mínima.
- [ ] Tests.
- [ ] Regresión + manifest parity + doctor.

## Dependencias
Ninguna externa. Reusa `sdd.chequear_limites`/`checks.py` tal cual.

## Próximo paso exacto
N/A.
