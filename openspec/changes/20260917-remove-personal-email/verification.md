# Verificación — 20260917-remove-personal-email

## Evidencia obtenida
- **Auditoría inicial**: `git grep -n "<email personal del autor>"` sobre todo el repo trackeado → 24 apariciones en 9 archivos: 20 en el campo `"usuario"` de `aprobaciones` de 5 `control.json` (4 cada uno: `multi-provider-adapters`, `harmessi-bench`, `provider-routing`, `fallback-and-handoffs`, `cross-provider-hardening`), 2 en la línea `- Usuario:` de `proposal.md` de `multi-provider-adapters` y `harmessi-bench`, 2 citando el propio patrón de un sweep de privacidad anterior en `verification.md` de `20260917-v0-4-release-hardening` y `20260917-v0-5-release-hardening` (no eran fugas nuevas, citaban el patrón buscado).
- **Causa raíz confirmada**: ningún código/template/lógica genera o auto-completa un email. `.claude/skills/lead-data-scientist/templates/proposal.md` línea 37 tiene solo `- Usuario:` sin valor por default. `tools/ds_guard.py` define `--usuario` como argumento obligatorio en `approve`/`remediation extend`/`decision add`/`decision supersede`/`decision revoke`, y lo persiste verbatim sin validación de forma. El email llegó a los artefactos porque la sesión orquestadora (el Lead) leyó el campo de contexto `userEmail` que el sistema provee para fines de autoría/atribución y lo interpretó de forma demasiado amplia para artefactos versionados y potencialmente públicos -- fue una decisión de la sesión, no un bug de generación automática.
- **`CITATION.cff`**: NO contiene ningún email (`grep -n "@" CITATION.cff` → 0 resultados). Solo tiene `family-names: "Colombo"` / `given-names: "Federico"` y URLs de GitHub sin email. No se tocó.
- **Fix de contenido**: los 20 valores de `"usuario"` en los 5 `control.json` corregidos a `"Federico Colombo"` (sin email, ningún otro campo tocado). Los 2 `proposal.md` corregidos a `- Usuario: Federico Colombo.`. Los 2 `verification.md` reformulados para no contener la cadena literal del email en ningún punto, preservando el significado sustantivo de lo que se buscó/encontró/decidió en su momento, y el de `20260917-v0-5-release-hardening` agrega una frase apuntando a la resolución en este mismo Change.
- **Guarda técnica agregada**: `tools/dsguard/core.py::validar_usuario_sin_email(usuario) -> Optional[str]` (heurística: si `"@" in usuario`, devuelve un mensaje de error citando el valor recibido; si no, `None`). Cableada como primera validación (antes de cualquier lectura/escritura persistente) en los 5 command handlers de `tools/ds_guard.py` que persisten un `usuario`: `cmd_approve`, `cmd_remediation_extend`, `cmd_decision_add`, `cmd_decision_supersede`, `cmd_decision_revoke` -- cada uno corta con `return 2` (error de uso/configuración) y un mensaje claro a stderr, sin escribir nada, si `--usuario` contiene `@`.
- **Documentación de la convención**: nota agregada en `.claude/skills/lead-data-scientist/sdd.md` (sección "Evidencia de aprobación") y `.claude/skills/lead-data-scientist/decision-ledger.md` (cerca de los comandos `decision add/supersede/revoke`): "usuario" es siempre un nombre humano, nunca un email, con referencia a la guarda técnica.
- **Regresión encontrada y corregida**: al agregar la nota a `.claude/skills/lead-data-scientist/decision-ledger.md`, se rompió la sincronización con su plantilla fuente `tools/ds_init/profiles/python_jupyter_data/templates/decision-ledger.md.tmpl` (test real `tools/tests/test_skill_methodology.py::TestSincronizacionTmplRenderizado`, que exige que ambos archivos sean byte-idénticos). Detectado corriendo la suite completa, corregido agregando el mismo bloque al `.tmpl` en la posición correcta -- confirmado con `diff` sin diferencias tras el fix.
- **Tests nuevos**: `tools/tests/test_validar_usuario_sin_email.py` (3 tests de la función pura) + `tools/tests/test_ds_guard_usuario_guard.py` (integración real con `argparse.Namespace` fabricado para `cmd_approve`/`cmd_decision_add`, confirma exit 2 y que no se escribe nada, más un chequeo estructural de que los 5 handlers llaman a la guarda antes de su primera escritura) → `10 passed` en `1.90s`.
- **Regresión completa**: `tools/tests` (incluye dsguard/core + el fix de sincronización) → `640 passed, 2 skipped, 32 subtests passed` en `674.48s` (0:11:14), 0 failures (1 falla real encontrada en la primera corrida por la desincronización del `.tmpl`, diagnosticada y corregida, luego confirmada en verde). `tools/ds_init/tests` → `112 passed` en `401.89s` (0:06:41). `tools/harmessi/tests` → `94 passed` en `342.65s` (0:05:42). Total: **846 passed, 2 skipped, 32 subtests, 0 failures** en las 3 suites afectadas.
- **Auditoría final (`git grep`)**: `git grep -n "<email personal del autor>"` → 0 resultados en todo el repo trackeado. `git grep -n -E "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}"` (regex amplia de cualquier email) → 20 resultados, los 20 son `test@example.com` usado como valor de fixture de `git config user.email` en la configuración de repos temporales de tests (`tools/ds_init/tests`, `tools/dsimpact/tests`, `tools/harmessi/tests`, `tools/tests`) -- dominio reservado para documentación/testing (RFC 2606), no un email real ni personal, sin relación con este hallazgo.
- **Privacidad transversal complementaria**: `grep` case-insensitive para `AGD`/`UNCO-Intelligence`/`Model-churn` y para rutas `C:\Users\fcolombo`/`C:\Datos` sobre todo el repo trackeado → mismos resultados que el sweep de Change 5 de v0.5 (solo referencias al propio patrón de búsqueda en verificaciones de hardening, 0 fugas reales, 0 rutas absolutas).

## Fingerprint de dataset/artefacto
No aplica -- corrección de privacidad/hardening del harness, no involucra datos de un proyecto DS.

## Diferencias contra la spec
Ninguna respecto a lo declarado en `proposal.md`. El único ajuste fue el fix adicional de sincronización `.tmpl`/renderizado, descubierto por la propia regresión y corregido en el mismo Change (no cambia el alcance declarado, es una consecuencia directa del fix ya previsto).

## Limitaciones
1. La guarda técnica (`"@" in usuario`) es una heurística simple, no un parser de email completo -- suficiente para el propósito (detectar la forma "nombre (email)" que causó el hallazgo), pero no exhaustiva contra cualquier variante de ofuscación deliberada.
2. El fix de contenido en `proposal.md` de `multi-provider-adapters` y `harmessi-bench` (Changes ya cerrados de v0.5) deja desactualizado el hash de aprobación de `proposal.md` registrado en sus respectivos `control.json` (que ya no tiene el email, corregido en este mismo Change, pero el hash fue calculado sobre el contenido ANTERIOR con el email) -- trade-off aceptado explícitamente: no había forma de eliminar el email sin invalidar ese hash, y el usuario pidió explícitamente la eliminación sin excepción.
3. El email personal sigue existiendo en el historial de commits de `v0.5-dev` (varios commits, ver reporte del Lead fuera de este `verification.md`) y YA está presente en `main`, que ya fue pusheado a `origin` (github.com/federcolombo/harmessi) desde antes de esta sesión (release de v0.4.0) -- limpiar historial es una decisión separada, explícitamente diferida por el usuario para después de este Change (no se reescribe ningún commit acá, según instrucción explícita).
4. `openspec/changes/20260917-v0-4-release-hardening/verification.md` es un artefacto de un Change de v0.4 ya cerrado y mergeado a `main` hace tiempo -- se corrigió igual porque el usuario pidió eliminar el email de "todos los archivos tracked actuales" sin excepción, pero es la corrección más alejada en el tiempo del Change que la originó.

## Pendientes derivados
Ninguno bloqueante para este Change. La limpieza de historial de `v0.5-dev` y de `main` (ya público) queda pendiente de decisión explícita del usuario, fuera del alcance de este Change (ver Limitaciones).

## Resultado final
**Email personal eliminado de los 24 puntos donde aparecía en archivos tracked (0 restantes, confirmado por git grep exacto y por regex amplia de email -- los únicos 20 resultados de la regex amplia son fixtures `test@example.com`, no relacionados). CITATION.cff confirmado sin email, no se tocó. Guarda técnica real agregada (`validar_usuario_sin_email`) en los 5 comandos de `ds_guard` que persisten un campo `usuario`, con tests de integración reales confirmando que rechaza un email con exit 2 sin escribir nada. Convención documentada en sdd.md/decision-ledger.md. Una regresión real (desincronización `.tmpl`/renderizado) fue encontrada por la propia suite y corregida en el mismo Change. 846 tests pasaron entre las 3 suites afectadas, 0 failures. El email sigue existiendo en el historial de commits de v0.5-dev y ya está en `main` (pusheado a origin desde el release de v0.4.0) -- queda como decisión separada del usuario, no resuelta acá por instrucción explícita de no tocar aún git config/metadata de commits. Listo para commit local separado, tal como pidió el usuario.**
