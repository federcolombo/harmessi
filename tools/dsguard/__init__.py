"""Paquete `dsguard`: núcleo del verificador determinista `ds_guard`.

Sin lógica propia acá — solo agrupa los módulos `core`, `repo`, `sdd` (y, en la
Sesión B, `notebooks`). Los subcomandos viven en `tools/ds_guard.py`.
"""
