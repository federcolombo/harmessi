"""Paquete `nbrunner`: núcleo del futuro `tools/notebook_runner.py`.

Sin lógica propia acá — solo agrupa los módulos `core` (reexporta utilidades de
`tools/dsguard/core.py`) y `manifest` (schema y validaciones previas a
ejecución). `execute.py`, `fsdiff.py` y el CLI son de sesiones futuras.
"""
