"""Paquete `harmessi`: CLI unificado de Harmessi. En v0.2.0 (Bloque 2)
expone únicamente el subcomando `doctor` (diagnóstico de salud, solo
lectura). `init` (el instalador, hoy `python -m tools.ds_init`), `update` y
`uninstall` quedan para versiones futuras -- ver `tools/ds_init` mientras
tanto.

Uso: `python -m tools.harmessi doctor [--destino <ruta>]`.
"""
